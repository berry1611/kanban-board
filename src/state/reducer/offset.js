const initialState = {
  offsetLeft: 0,
  offsetTop: 0,
};

const offsetReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default offsetReducer;
